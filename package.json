{
  "name": "course-allocation-system",
  "version": "1.0.0",
  "description": "AI Powered Student Course Allocation System",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npx tsc",
    "build": "npm run build:frontend && npm run build:backend",
    "start:backend": "cd backend && node dist/index.js",
    "start": "npm run start:backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
